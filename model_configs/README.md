#instructions

ensure have all dependences (torch, torchvision)

##expt1
MNIST dataset: labelled, images
batch: how many images processed at once (higher batch tends to give higher accuracy)
width: improve model (more weights/parameters):
       width= 64 =>  60K parameters
       width=256 => 300K parameters
       width=512 => 1.2M parameters